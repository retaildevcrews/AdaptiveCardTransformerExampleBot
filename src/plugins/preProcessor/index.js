/**
 * Given the payload and corresponding template, the preProcessor maniputes the data payload and prepares the data payload for template transformation.
 *
 * @param data DialogPayload
 * @param template Adaptive Card Template for the dialogPayload
 * @returns manipulated dialogPayload
 */
function preProcess(data, template) {
	// manipute dialogPayload here before applying template

	return data
}

module.exports = preProcess
